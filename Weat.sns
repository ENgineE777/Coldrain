#import "Main"

class WeatController
{
	Main@ main;

	void OnTouch(int self_index, string&in name, int index)
	{
		if (name == "Hero_Legs" && main.weat_on)
		{
			main.MakeCharacterDead(main.GetHero(), 0.0f, 1.0f);
		}
	}
}